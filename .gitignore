# Local .terraform directories
.terraform/

# .tfstate files and backups
*.tfstate
*.tfstate.*.backup

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data.
# Variables can be provided via other mechanisms, such as environment variables,
# or through a CI/CD system.
#
# If you must commit a tfvars file, use a specific name and uncomment the
# following line to ignore all others.
# # *tfvars
#
# If you want to ignore all tfvars file except a specific one (e.g. terraform.tfvars)
# uncomment the following lines
# # *.tfvars
# # !terraform.tfvars

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Optional: ignore plan files saved before destroying Terraform configuration
# Uncomment the line below if you want to ignore planout files.
# *tfplan*
